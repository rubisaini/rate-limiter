pipeline {
agent any
    stages {
        stage('install dependencies') {
            steps {
              sh  'npm insatll'
            }
        }
        stage('test') {
            steps {
              sh  'echo "test app"'
            }
        }
        stage('deploy') {
            steps {
              sh  'echo "deploy app"'
            }
        }
    }
}
